{
  "name": "monark-lens",
  "version": "2.0.0-dev.0",
  "private": true,
  "type": "module",
  "description": "Monark Lens — extension Chrome (réécriture v2, TS strict + Vite, MV3)",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "tsc --noEmit && vite build",
    "zip": "python3 scripts/zip.py"
  },
  "devDependencies": {
    "@types/chrome": "*",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
